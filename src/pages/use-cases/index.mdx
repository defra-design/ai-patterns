---
layout: ../../layouts/Layout.astro
title: Use case patterns
activeNav: use-cases
updated: 2026-01-08
---

import GridRow from '../../components/grid/GridRow.astro'
import GridColumn from '../../components/grid/GridColumn.astro'
import Card from '../../components/card/Card.astro'
import CardContainer from '../../components/card/CardContainer.astro'
import { getPatterns, getStatusTagClass } from '../../utils/patterns.js'

export const allPatterns = getPatterns(import.meta.glob('./*.md', { eager: true }))

<GridRow>
    <GridColumn width="two-thirds">
        # Use case patterns
        
        <p class="govuk-body-l">
            End-to-end patterns for building complete AI-powered services in Defra.
        </p>
        
        Use case patterns describe full system architectures and approaches for common AI applications such as:
        - Document classification and routing
        - Policy analysis and summarisation
        - Data extraction from forms and documents

        Each use case pattern references relevant [technical patterns](/technical/) for implementation details.
    </GridColumn>
</GridRow>

{allPatterns.length > 0 ? (
    <CardContainer>
        {allPatterns.map(pattern => (
            <Card title={pattern.frontmatter.title} href={pattern.url}>
                <div class="govuk-!-margin-bottom-2">
                    <strong class={`govuk-tag ${getStatusTagClass(pattern.frontmatter.status)}`}>
                        {pattern.frontmatter.status || 'Draft'}
                    </strong>
                </div>
                {pattern.frontmatter.description && (
                    <p class="govuk-body-s govuk-!-margin-bottom-2">
                        {pattern.frontmatter.description}
                    </p>
                )}
                {pattern.frontmatter.category && (
                    <p class="govuk-body-s govuk-!-margin-bottom-0 dfe-card-meta">
                        <strong>Category:</strong> {pattern.frontmatter.category}
                    </p>
                )}
            </Card>
        ))}
    </CardContainer>
) : (
    <GridRow>
        <GridColumn width="two-thirds">
            <p class="govuk-body govuk-!-margin-top-6">
                We are currently developing patterns for this section. Check back soon for updates.
            </p>
        </GridColumn>
    </GridRow>
)}

<GridRow>
    <GridColumn width="two-thirds">
        <p class="govuk-body-s govuk-!-margin-top-6">
            Last updated: {new Date(frontmatter.updated).toLocaleDateString('en-GB', { 
                day: 'numeric', 
                month: 'long', 
                year: 'numeric' 
            })}
        </p>
    </GridColumn>
</GridRow>
